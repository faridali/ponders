$("#slides").append("<%= j render(@slide) %>");
$('#slide_<%= @slide.id %>').effect("highlight", {color: 'rgba(80, 215, 226, 0.1)'}, 500);
$( '#new_slide' ).each(function(){
    this.reset();
});
window.scrollTo(0,document.body.scrollHeight);
$(document).ready(function(){
    $('c').tooltip();
});