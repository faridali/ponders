<%= form_for([@status,slide],:url=>status_slide_path(@status,slide), :method => :put, :html => {:multipart => true}) do |f| %><% if !slide.image? %>
      <%= f.label :image, "Upload image:" %>
      <%= f.file_field :image, name: "image[image]" %>
<% end %>
<% end %>